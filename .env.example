# Qdrant Configuration
QDRANT_URL=http://localhost:6333
# QDRANT_API_KEY=  # Optional, for Qdrant Cloud

# Embedding Model (FastEmbed ONNX models)
# Default: BAAI/bge-base-en-v1.5 (768 dims, 210MB, 44.52 MTEB, ~1s total search time)
EMBEDDING_MODEL=BAAI/bge-base-en-v1.5

# Alternative FastEmbed models:
# EMBEDDING_MODEL=BAAI/bge-small-en-v1.5  # 384 dims, 67MB, 43.76 MTEB, faster load (~0.5s)
# EMBEDDING_MODEL=BAAI/bge-large-en-v1.5  # 1024 dims, 1.2GB, 45.08 MTEB, slower load (~2s)

# Future: HuggingFace Inference API support (for top MTEB models without local loading)
# HF_TOKEN=your-huggingface-token
